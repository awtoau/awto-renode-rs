# State of the transpiler

Narrative counterpart to the generated `STATUS.md`. Every number is produced by
a committed script. Where something is unverified it says so.

## What it is

A C#-to-Rust transpiler driven by a corpus database. Renode is the corpus that
tells us which constructs matter — not the deliverable.

    frontend/RenodeIngest/     Roslyn walk -> SQLite corpus
    rulesdb/rules/*.json       the knowledge, as DATA
    scripts/emitter/lang/      generic C#, corpus-agnostic
    scripts/emitter/plugins/   corpus idioms, consulted FIRST
    src/csharp-rt/             types C# has and Rust lacks

`check_layering.py` fails the commit if the generic layer names a corpus
construct.

## Proven

**Generated UART replays 33,164 recorded accesses with zero divergences** —
indistinguishable from the hand-written file on the largest oracle available.

| trace | accesses | divergences | |
|---|---|---|---|
| usart1 | 33,164 | 0 | **100%** |
| exti | 25 | 0 | **100%** |
| dma1 | 183 | 7 | 92.8% |
| dma2 | 12,356 | 616 | 90.0% |
| adc1 | 16,800 | 1,192 | 89.6% |
| syscfg | 9 | 2 | 66.7% |
| gpioPortA | 62 | 14 | 17.6% |
| can1 | 228 | 99 | 13.9% |

Both DMA rows moved on 2026-08-02, from 9.3% and 0.2% (#55). The C# `Stream`
defines six registers into its **parent's** bank at `base + id * 0x18`, eight
times over; the register form matched only a *constant* offset, so 48 registers
were absent, every read returned 0, and **nothing reported a gap** — an offset
that matches no form does not exist as far as the walker is concerned. 5,629 of
6,252 divergences gone.

What remains is attributable to one gap, and only one: every surviving
divergence is `LowInterruptStatus` or `HighInterruptStatus`, whose per-stream
flags are bound by a `for` loop extending a builder held in a local. 0x20 is
bit 5 (TCIF0), 0x8000000 is bit 27 (TCIF3) — those flags and nothing else.

**The other low rows were low for a reason nobody had named.** An independent
audit comparing generated Rust against its C# line by line found that
`emit_registers` dropped, in silence, any call no rule matched and any register
left with no fields. So:

- **syscfg's register map was entirely empty.** `define_registers` was `{}`, and
  the file reported four gaps, none about registers. Its fields are bound by an
  *instance* method rather than the extension class the rule table names. The
  trace is 9 accesses with a ratchet of 2, so an empty map passed.
- **five of GPIO's eleven registers were absent** — Mode, OutputSpeed,
  PullUpPullDown, AlternateFunctionLow/High. Three reported "callback needs peer
  method", which is documented to mean *the layout is still correct*. It was not.
  Two reported nothing at all. gpioPortA diverges at exactly 0x0, 0x8 and 0xC.
- **two register-level `WithWriteCallback`s in UART** were dropped, on flags that
  are WriteZeroToClear — so clearing them never recomputes the IRQ line. usart1
  still scores 100% over 33,164 accesses, because a trace cannot observe an IRQ.

All three now report. This is why the earlier claim that GPIO's blocker was
peer-call attribution (#54) was wrong: #54 is real, but it was not what kept
those five registers out of the bank.

**Rules generalise.** Built against two peripherals, the converter emits **470
of 569 modules across all of Renode** that compile. Cut and whole-tree gap
distributions agree to **2.0 percentage points**. Rules mined from a
Python-to-Rust project transferred unchanged.

## Not proven

- **Compiling is not working.** c2rust emits >99.99% and 72.64% runs. 470
  modules compile; **8 have ever been executed**.
- **Replay proves indistinguishability on that trace only.** The invented
  `.with_reserved(9, 23)` survived 33,000 accesses.
- **Threading cannot currently be certified** (#52). Replay is single-threaded
  by construction.
- **No dynamic evidence.** `ownership.tsv` is static — declared types and
  access counts.

## The three checks

| check | proves | blind to |
|---|---|---|
| `check_generated` | reproducible from rules alone | whether the rules are right |
| `check_refactor` | a change altered no output | whether the output was ever right |
| `compile_check` | well-formed | whether it behaves |
| trace replay | matches recorded C# | anything the trace misses |
| reading the C# | everything above misses | nothing, and it does not scale |

Each found bugs the others could not. `x as i32 << 8` (400 errors, one
construct) came from compiling. `out arr[i]` binding to nothing — which
compiled, was reproducible, and produced no gap — came from replay.

**The last row is not a joke, and it earned its place on 2026-08-02.** All four
automated checks passed on a SYSCFG module whose register map was empty and a
GPIO module missing five registers. Reading the C# beside the Rust found both in
an afternoon. Every check above answers a question that is cheap to ask; none of
them asks *is this the same peripheral*.

`check_refactor` is the newest and had the same disease on its first day: it
recorded whatever the converter printed, including a crash, so a worktree with
no corpus produced a baseline of nine identical stack traces and reported
success. Writing the check is not the same as checking the check.

## The object graph

552 fields in the cut: 243 values, 94 arena handles, 73 callbacks, 47 shared,
19 back-references — **52 edges, 7 cycles**. Cycle participants in orange.

```mermaid
graph LR
    ActualLogger --> LogSourcesMap
    AlarmConfig --> STM32F4_RTC
    AlarmConfig --> TimerConfig
    BaseGPIOPort --> IMachine
    BaseRegisterCollection --> IPeripheral
    BaseRegisterCollection --> RegisterSelector
    BasicBytePeripheral --> IBusController
    BasicBytePeripheral --> IMachine
    BasicDoubleWordPeripheral --> IBusController
    BasicDoubleWordPeripheral --> IMachine
    BitBanding --> IBusController
    CaptureCompareChannel --> GPIO
    CaptureCompareChannel --> STM32_Timer
    CortexM --> NVIC
    DeviceRegisters --> BitTimingRegister
    DeviceRegisters --> ErrorStatusRegister
    DeviceRegisters --> FifoAssignmentRegister
    DeviceRegisters --> FilterActiveRegister
    DeviceRegisters --> FilterMasterRegister
    DeviceRegisters --> FilterModeRegister
    DeviceRegisters --> FilterScaleRegister
    DeviceRegisters --> InterruptEnableRegister
    DeviceRegisters --> MailboxIdentifierRegister
    DeviceRegisters --> MasterControlRegister
    DeviceRegisters --> MasterStatusRegister
    DeviceRegisters --> ReceiveFifoRegister
    DeviceRegisters --> TransmitStatusRegister
    GPIOAlternateFunction --> STM32_GPIOPort
    InternalReceiver --> STM32_SYSCFG
    MappedMemory --> IMachine
    MappedMemory --> SharedSegment
    MappedSegment --> MappedMemory
    NVIC --> CortexM
    NVIC --> ExceptionSimpleArray
    NVIC --> IMachine
    NVIC --> SecurityBanked
    PeripheralRegister --> IPeripheral
    RegisterField --> PeripheralRegister
    STM32DMA --> Stream
    STM32F4_FlashController --> MappedMemory
    STM32F4_RTC --> AlarmConfig
    STM32F4_RTC --> TimerConfig
    STM32_GPIOPort --> GPIOAlternateFunction
    STM32_Timer --> CaptureCompareChannel
    STM32_Timer --> IBusController
    STM32_Timer --> IMachine
    STMCAN --> DeviceRegisters
    STMCAN --> FilterBank
    STMCAN --> STMCAN
    Stream --> STM32DMA
    SysTick --> NVIC
    TimerConfig --> STM32F4_RTC
    classDef cyc fill:#f97316,stroke:#c2410c,color:#fff;
    class AlarmConfig,CaptureCompareChannel,CortexM,GPIOAlternateFunction,NVIC,STM32DMA,STM32F4_RTC,STM32_GPIOPort,STM32_Timer,STMCAN,Stream,TimerConfig cyc;
```

Generated by `scripts/prescan.py`; `ownership-tree.tsv` carries the per-field
classification with its evidence. (The diagram above was drawn from the corpus
cut, which has since been removed — `ownership-cut.tsv` no longer exists, and
prescan now writes the whole-tree figures quoted below.)

Across the whole tree: **1,278 edges, 63+ cycles, 1,690 reference fields.**

That difference is why the GC question is scope-dependent, and why #57 takes it
head-on: `Gc<T>` to make the port mechanical, a tracer to remove it again. The
tracer doubles as the dynamic evidence this project has never had.

## Progress

Generated lines against reported gaps, per commit. Both lines, deliberately:
generated output FELL from 545 to 523 when withholding-on-gap-markers landed,
and that was an improvement — methods carrying a `/* GAP */` marker stopped
being emitted, one of which was an infinite loop that compiled. A single
"progress" number would have shown that as a regression.

Regenerate with `scripts/progress_graph.py` (writes `tmp/progress.html`).

## What bit us

**Six paths produced no output and reported no reason** — indistinguishable
from a rule correctly declining, so review could not catch them. Two were
reported as landed before anyone noticed. Four came from editing by string
match against anchors moved by a refactor.

**Four bugs compiled cleanly and were wrong**: a self-call recursing forever, a
`/* GAP */` in a loop increment, an unmapped return type falling back to
`-> ()`, and `16.0` emitted as `16`.

The remedy was tooling, not discipline — `must_explain`, the wiring guard, the
ratchets. Each found something the day it landed.

## Open

| # | what | decided |
|---|---|---|
| #57 | `Gc<T>` + tracer | **yes** — supersedes D1 if it lands |
| #56 | inheritance | **re-decide**, including embed-vs-merge |
| #60 | interface traits | **full trait**; map the blocked types first (23 of 112 typable, not 67) |
| #52 | threading | **oracle first**, then settle D3 |
| #54 | peer-call type attribution | GPIO's blocker |
| *unfiled* | builder held in a local, extended in a loop | all 623 remaining DMA divergences |
| *unfiled* | unbound `WithValueField` emitted as a tag, so writes are ignored | 159 sites |
| #53 | instrumentation tail | five paths unaudited |
| #58 | C#/Rust semantics, measured | finalizers are a non-issue (0); `ref` params (40) unhandled |
| #59 | concepts from research never made into work | WASM oracle, feature reduction, repair loop |

**Still hand-written: 445 lines.** `uart.rs` and `gpio_port.rs`. Whole-peripheral
emission is what "zero patches" means, and it makes every decision above
load-bearing at once.
