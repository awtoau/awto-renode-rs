{
  "family": "REGISTER_FIELD",
  "description": "Renode's register DSL combinators. These are RULES, not emitter source code: the converter reads them from the database and knows nothing about any individual combinator. Adding support for a new one is a data change.",
  "bindings": {
    "pos": "position",
    "width": "width",
    "mode": "mode",
    "field": "$out",
    "provider": "$has:valueProviderCallback",
    "writer": "$has:writeCallback"
  },
  "rules": [
    {
      "name": "WITH_FLAG_BOUND",
      "matches": "WithFlag",
      "when": "field",
      "emit": ".with_flag({pos}, &mut f.{field}, {mode})"
    },
    {
      "name": "WITH_FLAG_COMPUTED",
      "matches": "WithFlag",
      "when": "provider or writer",
      "emit": ".with_tagged_flag({pos})",
      "gap": "computed field: needs a dispatch arm",
      "note": "A callback with no `out` parameter has no storage. Emitting a field handle here would fabricate one, so the rule emits a tag and reports the gap."
    },
    {
      "name": "WITH_FLAG_TAG",
      "matches": "WithFlag",
      "emit": ".with_tagged_flag({pos})"
    },
    {
      "name": "WITH_VALUE_BOUND",
      "matches": "WithValueField|WithEnumField",
      "when": "field",
      "emit": ".with_value({pos}, {width}, &mut f.{field}, {mode})"
    },
    {
      "name": "WITH_VALUE_COMPUTED",
      "matches": "WithValueField|WithEnumField",
      "when": "provider or writer",
      "emit": ".with_tag({pos}, {width})",
      "gap": "computed field: needs a dispatch arm"
    },
    {
      "name": "WITH_VALUE_TAG",
      "matches": "WithValueField|WithEnumField",
      "emit": ".with_tag({pos}, {width})"
    },
    {
      "name": "WITH_FIELDS_BOUND",
      "matches": "WithValueFields|WithEnumFields",
      "when": "field",
      "emit": ".with_fields_cb({pos}, {width}, {count}, &mut f.{field}, {mode}, None, None)",
      "bindings": {
        "count": "count"
      }
    },
    {
      "name": "WITH_TAGGED_FLAG",
      "matches": "WithTaggedFlag",
      "emit": ".with_tagged_flag({pos})"
    },
    {
      "name": "WITH_TAG",
      "matches": "WithTag",
      "emit": ".with_tag({pos}, {width})"
    },
    {
      "name": "WITH_RESERVED_BITS",
      "matches": "WithReservedBits",
      "emit": ".with_reserved({pos}, {width})"
    },
    {
      "name": "REGISTER_CALLBACK",
      "matches": "WithWriteCallback|WithReadCallback|WithChangeCallback",
      "emit": null,
      "gap": "register-level callback: dispatch, not layout",
      "note": "Deliberately emits nothing. These carry behaviour, not register shape, and a plausible-looking guess would leave the oracle checking generated guesswork."
    }
  ],
  "register_forms": [
    {
      "name": "DEFINE_EXTENSION",
      "note": "`Register.Status.Define(this, 0xC0, name: \"USART_SR\")` -- the offset is the enum member passed as the first argument, which Roslyn records as a constant.",
      "symbol_contains": ".Define(",
      "offset_from": "$first_argument_enum",
      "reset_from": "resetValue",
      "chain_from": "$self"
    },
    {
      "name": "DICTIONARY_ADD",
      "note": "`{(long)Registers.Mode, new DoubleWordRegister(this).With...}` -- a dictionary initialiser, which desugars to Dictionary.Add(key, value). Offset is the key; the combinator chain is inside the value.",
      "symbol_contains": "Dictionary<TKey, TValue>.Add",
      "offset_from": "key",
      "reset_from": null,
      "chain_from": "value"
    }
  ],
  "expressions": [
    {
      "name": "FLAG_FIELD_READ",
      "note": "`someFlag.Value`. The symbol normalises to IRegisterField<T>.Value because call targets are keyed on OriginalDefinition, so flag-vs-value is distinguished by the RESULT TYPE, not the interface name.",
      "kind": "PropertyReference",
      "symbol_contains": "IRegisterField<T>.Value",
      "type_is": "bool",
      "emit": "self.bank.flag(self.f.{field})"
    },
    {
      "name": "VALUE_FIELD_READ",
      "kind": "PropertyReference",
      "symbol_contains": "IRegisterField<T>.Value",
      "emit": "self.bank.value(self.f.{field})"
    },
    {
      "name": "IRQ_SET",
      "note": "`IRQ.Set(x)` drives the interrupt line. The GPIO object is not modelled yet, so this assigns the peripheral's own flag.",
      "kind": "Invocation",
      "symbol_contains": "GPIO.Set(bool)",
      "emit": "self.irq = {arg0}"
    }
  ],
  "assignments": [
    {
      "name": "FLAG_FIELD_WRITE",
      "note": "`someFlag.Value = x`. Rust has no assignable property, and D2's handles are indices, so this is a bank call rather than an lvalue -- which is why assignment needs a PROJECT rule and not just the generic `{target} = {value}`.",
      "target_kind": "PropertyReference",
      "target_symbol_contains": "IRegisterField<T>.Value",
      "target_type_is": "bool",
      "emit": "self.bank.set_flag(self.f.{field}, {value});"
    },
    {
      "name": "VALUE_FIELD_WRITE",
      "target_kind": "PropertyReference",
      "target_symbol_contains": "IRegisterField<T>.Value",
      "emit": "self.bank.set_value(self.f.{field}, {value});"
    }
  ],
  "callback_signatures": {
    "note": "The DSL's callback types. A lambda bound to one of these parameters is emitted as a free fn with this signature; `bank` and `state` are what the C# closure captured via `this`.",
    "valueProviderCallback": {
      "params": "bank: &Bank<S>, st: &mut S, _idx: usize, _current: u64",
      "ret": "u64"
    },
    "writeCallback": {
      "params": "bank: &Bank<S>, st: &mut S, _idx: usize, _old: u64, new: u64",
      "ret": "()"
    }
  }
}
